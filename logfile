LOG:  database system was shut down at 2017-02-24 06:16:33 UTC
LOG:  MultiXact member wraparound protections are now enabled
LOG:  database system is ready to accept connections
LOG:  autovacuum launcher started
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		username        varchar(16),
		password        varchar(16)  /* Plaintext password  */
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		asset_tag	varchar(16),
		description	text
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		fcode 		varchar(6),
		common_name	varchar(32)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16)  /* Plaintext password  */
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		asset_tag	varchar(16),
		description	text
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		fcode 		varchar(6),
		common_name	varchar(32)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16)  /* Plaintext password  */
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		asset_tag	varchar(16),
		description	text
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		fcode 		varchar(6),
		common_name	varchar(32)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16)  /* Plaintext password  */
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		asset_tag	varchar(16),
		description	text
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		fcode 		varchar(6),
		common_name	varchar(32)
	);
ERROR:  database "lost" is being accessed by other users
DETAIL:  There are 6 other sessions using the database.
STATEMENT:  DROP DATABASE lost;
ERROR:  database "lost" is being accessed by other users
DETAIL:  There are 5 other sessions using the database.
STATEMENT:  DROP DATABASE lost;
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16)  /* Plaintext password  */
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		asset_tag	varchar(16),
		description	text
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		fcode 		varchar(6),
		common_name	varchar(32)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16)  /* Plaintext password  */
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		asset_tag	varchar(16),
		description	text
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		fcode 		varchar(6),
		common_name	varchar(32)
	);
LOG:  database system was shut down at 2017-02-25 00:02:29 UTC
LOG:  MultiXact member wraparound protections are now enabled
LOG:  database system is ready to accept connections
LOG:  autovacuum launcher started
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16)  /* Plaintext password  */
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  column "asset_pk" referenced in foreign key constraint does not exist
STATEMENT:  CREATE TABLE asset_at (
		asset_fk 	integer REFERENCES assets (asset_pk) not null,
		facility_fk     integer REFERENCES facilities (facility_pk) not null,
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16)  /* Plaintext password  */
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  column "asset_pk" referenced in foreign key constraint does not exist
STATEMENT:  CREATE TABLE asset_at (
		asset_fk 	integer REFERENCES assets (asset_pk) not null,
		facility_fk     integer REFERENCES facilities (facility_pk) not null,
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16)  /* Plaintext password  */
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  column "asset_pk" referenced in foreign key constraint does not exist
STATEMENT:  CREATE TABLE asset_at (
		asset_fk 	integer REFERENCES assets (asset_pk) not null,
		facility_fk     integer REFERENCES facilities (facility_pk) not null,
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16)  /* Plaintext password  */
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  column "asset_pk" referenced in foreign key constraint does not exist
STATEMENT:  CREATE TABLE asset_at (
		asset_fk 	integer REFERENCES assets (asset_pk),
		facility_fk     integer REFERENCES facilities (facility_pk),
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16)  /* Plaintext password  */
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16)  /* Plaintext password  */
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16)  /* Plaintext password  */
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  column "asset_pk" referenced in foreign key constraint does not exist
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk),
		facility_fk     integer REFERENCES facilities (facility_pk),
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16)  /* Plaintext password  */
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  column "asset_pk" referenced in foreign key constraint does not exist
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk),
		facility_fk     integer REFERENCES facilities (facility_pk),
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  database "lost" is being accessed by other users
DETAIL:  There are 4 other sessions using the database.
STATEMENT:  DROP DATABASE lost;
ERROR:  database "lost" is being accessed by other users
DETAIL:  There are 4 other sessions using the database.
STATEMENT:  DROP DATABASE lost;
LOG:  unexpected EOF on client connection with an open transaction
LOG:  received smart shutdown request
LOG:  autovacuum launcher shutting down
LOG:  received SIGHUP, reloading configuration files
LOG:  shutting down
LOG:  database system is shut down
LOG:  database system was shut down at 2017-02-25 07:34:27 UTC
LOG:  MultiXact member wraparound protections are now enabled
LOG:  database system is ready to accept connections
LOG:  autovacuum launcher started
ERROR:  relation "users" does not exist at character 15
STATEMENT:  select * from users;
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16)  /* Plaintext password  */
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk),
		facility_fk     integer REFERENCES facilities (facility_pk),
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16)  /* Plaintext password  */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		user_fk		serial primary key, 	/* primary key for a role instance*/
		title 		varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk),
		facility_fk     integer REFERENCES facilities (facility_pk),
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
LOG:  received smart shutdown request
LOG:  autovacuum launcher shutting down
FATAL:  terminating connection due to administrator command
LOG:  received SIGHUP, reloading configuration files
LOG:  shutting down
LOG:  database system is shut down
LOG:  database system was shut down at 2017-02-25 13:00:22 UTC
FATAL:  the database system is starting up
LOG:  MultiXact member wraparound protections are now enabled
LOG:  database system is ready to accept connections
LOG:  autovacuum launcher started
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  column "role_pk" does not exist at character 8
STATEMENT:  SELECT role_pk FROM roles WHERE rolename='facilities_officer'
ERROR:  column "role_pk" does not exist at character 8
STATEMENT:  SELECT role_pk FROM roles WHERE rolename='facilities_officer'
ERROR:  current transaction is aborted, commands ignored until end of transaction block
STATEMENT:  SELECT * FROM users WHERE username='je'
ERROR:  database "lost" is being accessed by other users
DETAIL:  There are 2 other sessions using the database.
STATEMENT:  DROP DATABASE lost;
LOG:  received smart shutdown request
LOG:  autovacuum launcher shutting down
LOG:  received SIGHUP, reloading configuration files
LOG:  shutting down
LOG:  database system is shut down
LOG:  database system was shut down at 2017-02-27 07:38:25 UTC
LOG:  MultiXact member wraparound protections are now enabled
LOG:  database system is ready to accept connections
LOG:  autovacuum launcher started
ERROR:  relation "roles" does not exist
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  invalid input syntax for integer: "{}" at character 66
STATEMENT:  INSERT INTO users (username,password,role_fk) VALUES ('je','123','{}')
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  invalid input syntax for integer: "{}" at character 66
STATEMENT:  INSERT INTO users (username,password,role_fk) VALUES ('je','123','{}')
ERROR:  current transaction is aborted, commands ignored until end of transaction block
STATEMENT:  SELECT * FROM users WHERE username='je'
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  null value in column "role_fk" violates not-null constraint
DETAIL:  Failing row contains (1, 123, 123, null).
STATEMENT:  INSERT INTO users (username,password) VALUES ('123','123')
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  invalid input syntax for integer: "{}" at character 69
STATEMENT:  INSERT INTO users (username,password,role_fk) VALUES ('asdf','asdf','{}')
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  column "role" of relation "users" does not exist at character 38
STATEMENT:  INSERT INTO users (username,password,role) VALUES ('11','11','{}')
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  invalid input syntax for integer: "{}" at character 63
STATEMENT:  INSERT INTO users (username,password,role_fk) VALUES ('1','1','{}')
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  invalid input syntax for integer: "{}" at character 67
STATEMENT:  INSERT INTO users (username,password,role_fk) VALUES ('111','111','{}')
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  invalid input syntax for integer: "{}" at character 63
STATEMENT:  INSERT INTO users (username,password,role_fk) VALUES ('1','2','{}')
ERROR:  current transaction is aborted, commands ignored until end of transaction block
STATEMENT:  SELECT * FROM users WHERE username='1'
ERROR:  current transaction is aborted, commands ignored until end of transaction block
STATEMENT:  SELECT count(*) FROM users WHERE username='' and password=''
ERROR:  current transaction is aborted, commands ignored until end of transaction block
STATEMENT:  SELECT count(*) FROM users WHERE username='123' and password='123'
ERROR:  current transaction is aborted, commands ignored until end of transaction block
STATEMENT:  SELECT count(*) FROM users WHERE username='123' and password=''
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  invalid input syntax for integer: "{}" at character 68
STATEMENT:  INSERT INTO users (username,password,role_fk) VALUES ('asdf','123','{}')
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  null value in column "role_fk" violates not-null constraint
DETAIL:  Failing row contains (2, 1, 1, null).
STATEMENT:  INSERT INTO users (username,password,role_fk) VALUES ('1','1',NULL)
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  invalid input syntax for integer: "{}" at character 63
STATEMENT:  INSERT INTO users (username,password,role_fk) VALUES ('1','1','{}')
ERROR:  invalid input syntax for integer: "{}" at character 67
STATEMENT:  INSERT INTO users (username,password,role_fk) VALUES ('123','123','{}')
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  invalid input syntax for integer: "{}" at character 66
STATEMENT:  INSERT INTO users (username,password,role_fk) VALUES ('je','123','{}')
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  invalid input syntax for integer: "{}" at character 61
STATEMENT:  INSERT INTO users (username,password,role_fk) VALUES ('','','{}')
ERROR:  current transaction is aborted, commands ignored until end of transaction block
STATEMENT:  SELECT common_name FROM facilities WHERE common_name=''
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  invalid input syntax for integer: "{}" at character 67
STATEMENT:  INSERT INTO users (username,password,role_fk) VALUES ('123','123','{}')
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  invalid input syntax for integer: "{}" at character 65
STATEMENT:  INSERT INTO users (username,password,role_fk) VALUES ('aa','aa','{}')
ERROR:  current transaction is aborted, commands ignored until end of transaction block
STATEMENT:  SELECT username FROM users WHERE username='asdf'
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  invalid input syntax for integer: "{}" at character 73
STATEMENT:  INSERT INTO users (username,password,role_fk) VALUES ('asdf','sadfasdf','{}')
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	text			/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  invalid input syntax for integer: "{}" at character 68
STATEMENT:  INSERT INTO users (username,password,role_fk) VALUES ('123','sadf','{}')
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (      /* table with username and password */
		user_pk		serial primary key,
		username        varchar(16),
		password        varchar(16),  /* Plaintext password  */
		role_fk		integer REFERENCES roles(role_pk) not null /* role foreign key */
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		role_pk		serial primary key, 	/* primary key for a role instance*/
		rolename	varchar(16)		/* short textual name for the role*/
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (				/*table with assets */
		asset_pk	serial primary key,	
		asset_tag	varchar(16),		/* 16 characters in length */
		description	text			/* arbitrary length */
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk 	serial primary key,
		fcode 		varchar(6),		/* facility code */
		common_name	varchar(32)		/* 32 characters in length */
	);
ERROR:  relation "asset_at" already exists
STATEMENT:  CREATE TABLE asset_at (
		asset_fk        integer REFERENCES assets (asset_pk) not null, /* asset at a facility  */
		facility_fk     integer REFERENCES facilities (facility_pk) not null, /* facility the asset is at*/
		arrive_dt       timestamp, 
		depart_dt       timestamp 
	);
ERROR:  invalid input syntax for integer: "{}" at character 63
STATEMENT:  INSERT INTO users (username,password,role_fk) VALUES ('1','1','{}')
LOG:  database system was shut down at 2017-02-27 11:59:17 UTC
LOG:  MultiXact member wraparound protections are now enabled
LOG:  database system is ready to accept connections
LOG:  autovacuum launcher started
ERROR:  invalid input syntax for integer: "{}" at character 64
STATEMENT:  INSERT INTO users (username,password,role_fk) VALUES ('12','1','{}')
ERROR:  invalid input syntax for integer: "{}" at character 64
STATEMENT:  INSERT INTO users (username,password,role_fk) VALUES ('12','1','{}')
ERROR:  invalid input syntax for integer: "{}" at character 63
STATEMENT:  INSERT INTO users (username,password,role_fk) VALUES ('12','','{}')
ERROR:  invalid input syntax for integer: "{}" at character 65
STATEMENT:  INSERT INTO users (username,password,role_fk) VALUES ('12','11','{}')
ERROR:  database "lost" is being accessed by other users
DETAIL:  There are 4 other sessions using the database.
STATEMENT:  DROP DATABASE lost;
ERROR:  database "lost" is being accessed by other users
DETAIL:  There are 4 other sessions using the database.
STATEMENT:  DROP DATABASE lost;
LOG:  received smart shutdown request
LOG:  autovacuum launcher shutting down
LOG:  received SIGHUP, reloading configuration files
LOG:  shutting down
LOG:  database system is shut down
